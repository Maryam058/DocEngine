# Daily Summary

## 2026-09-01

- Shipped the "QA for unpublished documentation drafts" feature: `api/agent-skill.py` now accepts an optional `document_content` field to run Documentation QA against draft content (via an ephemeral temp file, never written to `docs/`) instead of requiring a published page, and returns `source`/`published_baseline` metadata. `docs/javascripts/agent-panel.js` now sends the current editor content (converted from Quill HTML to Markdown via `PageChrome.htmlToMarkdown()`) alongside `document_path`, fixing a latent bug where the panel referenced `window.AppState` instead of the shared-scope `AppState` global.
- Verified with Python syntax checks, Node syntax checks, and the deterministic `run_checks.py` regression (8/8 pass) before committing.
- Committed (`b61b6cd`, "feat: support QA for unpublished documentation drafts") and pushed to `main`. GitHub Actions "Build documentation" workflow completed successfully for this commit.
- Diagnosed a production Groq 403 incident on `/api/agent-skill` and `/api/ai-suggest`: existing `describe_groq_error()` logic in both files hardcoded a "model permission" message for any 403 without reliably surfacing Groq's real error body, and the existing log line showed `message='(unparseable error body)'`, meaning the true cause was still unknown. Added a small, diagnostic-only logging helper to each file that safely logs Groq's parsed `error.message`/`error.type`/`error.code` (or a truncated, redacted raw body if non-JSON) to Vercel Function logs, with no change to the model, API key, retry logic, or the frontend-facing error message. Covered with a new stdlib `unittest` regression suite (`tests/test_groq_error_diagnostics.py`, 9/9 passing). Not yet committed/pushed, pending the user's review of the diff and next production log capture.
- Root-caused and fixed a production Groq HTTP 413 on `/api/ai-suggest` (8,948 tokens vs. an 8,000 TPM limit): `docs/style-guide.md`/`docs/glossary.md` were sent to Groq in full on every request with no size cap. Built a reusable, priority-ordered prompt-budget system (`estimate_tokens`, `allocate_chars`, `select_relevant_excerpt`) capping the assembled prompt at 6,000 estimated input tokens (24,000 chars), prioritizing selected text, then surrounding context, then a bounded/relevant excerpt of the style guide, then the glossary — so future growth in any one component can no longer exceed the Groq limit. Left `GROQ_API_KEY`/`GROQ_MODEL`/endpoint/Cloudflare headers/retry/timeout untouched; confirmed `api/agent-skill.py` already has its own adequate truncation and did not modify it. Added 20 new regression tests (33/33 total passing). Not committed or pushed, per instruction, pending user review.
